{{- range .Values.persistentVolumeClaims }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .name }}
  namespace: {{ $.Release.Namespace }}
spec:
  accessModes:
    {{- toYaml .accessModes | nindent 4 }}
  resources:
    requests:
      storage: {{ .storage | required "storage is required" }}
  volumeMode: Filesystem
  storageClassName: {{ .storageClassName | required "storageClassName is required" }}
  volumeName: {{ .volumeName | required "volumeName is required" }}
---
{{- end }}