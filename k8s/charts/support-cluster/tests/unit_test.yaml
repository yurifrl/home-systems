suite: test support-cluster chart
templates:
  - tailscale/connector.yaml
  - monitoring/cloudflare-tunnel.yaml
  - monitoring/victoriametrics-watchdog.yaml
  - longhorn-node-scheduling.yaml
tests:
  - it: should render connector correctly
    template: tailscale/connector.yaml
    values:
      - ../test.yaml
    asserts:
      - isKind:
          of: Connector
      - equal:
          path: spec.hostname
          value: tailscale-1
      - equal:
          path: spec.subnetRouter.advertiseRoutes[0]
          value: "10.244.0.0/16"
      - contains:
          path: spec.tags
          content: tag:k8s

  - it: should not treat missing Cloudflare metrics as a tunnel outage
    template: monitoring/cloudflare-tunnel.yaml
    documentIndex: 2
    asserts:
      - equal:
          path: spec.groups[0].rules[0].expr
          value: sum(cloudflared_tunnel_ha_connections) == 0

  - it: should render the independent VictoriaMetrics watchdog
    template: monitoring/victoriametrics-watchdog.yaml
    asserts:
      # Deployment, NOT CronJob: a CronJob churning one pod/min grew VM storage
      # ~250MB/day via kube-state-metrics pod-UID series (2026-08-29 incident).
      - isKind:
          of: Deployment
      - matchRegex:
          path: spec.template.spec.containers[0].args[0]
          pattern: "vmalertmanager-vmks:9093/api/v2/alerts"
      - matchRegex:
          path: spec.template.spec.containers[0].args[0]
          pattern: 'vm_free_disk_space_bytes/'

  - it: should enforce replica scheduling on every longhorn node
    template: longhorn-node-scheduling.yaml
    documentIndex: 3
    asserts:
      - isKind:
          of: CronJob
      - equal:
          path: spec.schedule
          value: "*/30 * * * *"
      - equal:
          path: spec.concurrencyPolicy
          value: Forbid
      - matchRegex:
          path: spec.jobTemplate.spec.template.spec.containers[0].command[2]
          pattern: 'allowScheduling'
      - equal:
          path: spec.jobTemplate.spec.template.spec.containers[0].env[0].value
          value: "[]"
