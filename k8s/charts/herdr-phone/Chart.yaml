apiVersion: v2
name: herdr-phone
description: Cloudflare DNS + Access for herdr.syscd.live (herdr-phone runs cloudflared on the Mac; this chart manages the account-side resources via crossplane)
type: application
version: 0.1.0
appVersion: "1.0.0"
