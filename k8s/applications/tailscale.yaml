apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: tailscale
  namespace: argocd
  finalizers:
  - resources-finalizer.argocd.argoproj.io
  annotations:
    argocd.argoproj.io/sync-wave: "-3"
spec:
  project: default
  destination:
    server: https://kubernetes.default.svc
    namespace: tailscale
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true 
  sources:
    - repoURL: https://pkgs.tailscale.com/helmcharts
      chart: tailscale-operator
      targetRevision: 1.98.9
      helm:
        valuesObject:
          oauthSecretVolume:
            secret:
              secretName: tailscale-operator-oauth
          # In-process API server proxy (auth mode): exposes kube-apiserver
          # over the tailnet at tailscale-operator:443. Requests are
          # impersonated with the sender's tailnet identity; Kubernetes RBAC
          # still governs what each identity can do.
          apiServerProxyConfig:
            mode: "true"
        
    - repoURL: https://github.com/yurifrl/home-systems.git
      targetRevision: HEAD
      path: k8s/charts/support
      helm:
        valuesObject:
          externalSecrets:
            - name: tailscale-operator-oauth

