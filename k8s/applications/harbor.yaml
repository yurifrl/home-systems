apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: harbor
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  destination:
    server: https://kubernetes.default.svc
    namespace: harbor
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
  source:
    repoURL: https://helm.goharbor.io
    chart: harbor
    targetRevision: "1.17.0"
    helm:
      values: |
        # Harbor basic configuration
        externalURL: https://registry.syscd.live

        # Use Longhorn for persistence
        persistence:
          enabled: true
          resourcePolicy: keep
          persistentVolumeClaim:
            registry:
              storageClass: longhorn
              size: 20Gi
            chartmuseum:
              storageClass: longhorn
              size: 5Gi
            jobservice:
              storageClass: longhorn
              size: 1Gi
            database:
              storageClass: longhorn
              size: 5Gi
            redis:
              storageClass: longhorn
              size: 1Gi
            trivy:
              storageClass: longhorn
              size: 5Gi

        # Database - use internal PostgreSQL
        database:
          internal:
            username: postgres
            password: changeme
            # Using internal DB - no external dependencies

        # Redis - use internal
        redis:
          internal:
            password: changeme

        # Disable components you don't need
        notary:
          enabled: false

        # Trivy scanner - lightweight vulnerability scanning
        trivy:
          enabled: true

        # Proxy cache - disabled for now
        proxy:
          enabled: false

        # Internal TLS disabled (handled by Istio/ingress)
        internalTLS:
          enabled: false

        # Metrics and tracing disabled
        metrics:
          enabled: false

        # Logging level
        logLevel: info

        # Minimal resource allocation for homelab
        nginx:
          resources:
            requests:
              cpu: 50m
              memory: 128Mi
            limits:
              cpu: 200m
              memory: 256Mi

        portal:
          resources:
            requests:
              cpu: 50m
              memory: 128Mi
            limits:
              cpu: 200m
              memory: 256Mi

        core:
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi

        jobservice:
          resources:
            requests:
              cpu: 50m
              memory: 128Mi
            limits:
              cpu: 200m
              memory: 256Mi

        registry:
          controller:
            resources:
              requests:
                cpu: 50m
                memory: 64Mi
              limits:
                cpu: 100m
                memory: 128Mi
          registry:
            resources:
              requests:
                cpu: 50m
                memory: 128Mi
              limits:
                cpu: 200m
                memory: 256Mi
---
# VirtualService for Harbor registry (via support chart)
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: harbor-ingress
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  destination:
    server: https://kubernetes.default.svc
    namespace: harbor
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
  source:
    repoURL: https://github.com/yurifrl/home-systems.git
    targetRevision: HEAD
    path: k8s/charts/support
    helm:
      valuesObject:
        virtualServices:
          - name: harbor
            service:
              name: harbor-core
              namespace: harbor
              port: 80
