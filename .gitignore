*.xz
# Talos PXE secrets dir at repo root (legacy). Don't ignore Go source dirs named 'secrets' inside .submodules/.
/secrets.*
/secrets
.DS_Store
*.env
talos/op
talos/op*
keys.conf
hack/foundryvtt-mcp
hack/foundry

# nostos cache — regenerable from config.yaml + templates/ + 1Password
nostos/state/

# Locally built binaries (e.g. nostos)
.bin/

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
.beads/proxieddb/
